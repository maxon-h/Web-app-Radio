import { FavoriteStationPipe } from './favorite-station.pipe';

describe('FavoriteStationPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteStationPipe();
    expect(pipe).toBeTruthy();
  });
});
